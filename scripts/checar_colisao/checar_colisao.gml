/// Script com o intuito de checar a colisão com outro objeto (no caso inimigo)

/*function checar_colisao(src, target) {
    if (place_meeting(src,x, src.y, target)) {
        return true
   } else {
    return false
   }
}